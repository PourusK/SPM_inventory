# Neon Postgres connection string (Dashboard -> Connect)
DATABASE_URL="postgresql://user:password@ep-xxxx.neon.tech/spminventory?sslmode=require"

# Auth.js
AUTH_SECRET="generate-with: npx auth secret"

# Anthropic (used for PDF/Excel/image machinery extraction)
ANTHROPIC_API_KEY="sk-ant-..."

# Vercel Blob (Vercel dashboard -> Storage -> Blob -> create store -> copy token)
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
